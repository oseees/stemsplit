# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/routewise?schema=public"

# NextAuth / Auth.js — generate with: npx auth secret
AUTH_SECRET="replace-with-a-long-random-string"
