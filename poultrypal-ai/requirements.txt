# PoultryPal AI — backend dependencies
# Deliberately light + Python 3.14-safe. No torch/onnxruntime by default;
# the knowledge store uses a dependency-free TF-IDF embedder (see app/knowledge/store.py).
fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.49
python-multipart>=0.0.9
numpy>=2.1
pypdf>=4.2          # optional: only used when ingesting PDF sources
python-dotenv>=1.0
