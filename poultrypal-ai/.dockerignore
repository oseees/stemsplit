# Keep the build context small and reproducible.
# NOTE: do NOT ignore *.md — the knowledge sources in app/knowledge/sources/ are .md.
.venv/
.venv-asr/
venv/
__pycache__/
*.pyc
.env
data/
asr/
# Built locally; the image rebuilds it from sources at build time.
app/knowledge/index/*.json
.git
.gitignore
.DS_Store
README.md
run.sh
