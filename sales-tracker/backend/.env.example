# Copy to .env and fill in to enable Claude AI advice + weekly reports.
# Everything else (sales, invoices, expenses, profit) works without this.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# SALESPAL_MODEL=claude-opus-4-8
# SALESPAL_DB=/absolute/path/to/salespal.db
