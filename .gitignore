# Unrelated sibling projects that live in this workspace but are NOT part of the
# tracked repo. Listed so an accidental `git add -A` from a subdir can never
# sweep them into a commit again (they each have their own home/repo).
.roo/
autosense_ai/
beat-uploader/
image-converter/
instagram-downloader/
mechanic-app/
nairabulk/
poultrypal-ai/
rentscout-lagos/
routewise/
supplier-tracker/
track2midi/
viralclip-ai/
vocalmatch/
vocalperfect/
yt-downloader/
expense-tracker/
random/

# Secrets. This repo is PUBLIC — never let these near a commit.
# .env.example files stay tracked (they hold no real values).
.env
.env.*
!.env.example
*.keystore
*.jks
keystore.pass

# Local venvs and build spew
venv/
.venv/
__pycache__/
